# Phase 3 manual browser smoke checklist

> This is a **manual, non-automated** browser checklist. It is not Playwright/Selenium coverage and must be completed against a clean local stack at `http://localhost:8000`. Record observed facts and failures; do not mark an item passed from an expected result alone.

| Check | Prerequisite | Action | Expected | Observed |
| --- | --- | --- | --- | --- |
| Clean bootstrap | Clean checkout; project volumes removed only after confirming they are disposable | Run dependency bootstrap and `docker compose up -d --wait`; run migrations. Confirm `api/public/storage` is untracked and, if it exists, Laravel's symbolic link; remove only that link with `storage:unlink`, then run `storage:link`. | Services become healthy; dependency volumes begin empty and are populated only by explicit installs; no migration, storage link, seed, or administrator is created by Compose startup. | Command evidence refreshed 2026-08-13: the empty `web_node_modules` volume was explicitly installed from the lockfile; an untracked Laravel storage link was deleted and recreated with `storage:unlink`/`storage:link`. No credential-gated browser item was observed. See `PHASE_3_VERIFICATION.md`. |
| Development administrator | Healthy API and an interactive local terminal | Run `docker compose exec api php artisan portfolio:bootstrap-admin`; retain credentials only locally | Administrator exists; no credential is committed or copied to documentation | Not observed: no interactive password was supplied; credentials must never enter commands, environment, history or this record. |
| Root locale redirect | Healthy gateway | Navigate to `/` in a new browser context | Redirect resolves to a valid localized route according to locale policy | Observed 2026-08-13: `/` redirected to `/es`. |
| Spanish page | Healthy gateway | Navigate to `/es` | Next.js Spanish shell renders without gateway error | Observed 2026-08-13: Spanish shell rendered with `lang=es`; no captured warning/error console entry. |
| English page | Healthy gateway | Navigate to `/en` | Next.js English shell renders without gateway error | Observed 2026-08-13: English shell rendered with `lang=en`; no captured warning/error console entry. |
| Exact public API | Healthy gateway | Navigate to `/api/v1` | Laravel JSON envelope identifies `ok` and `v1` | Automated HTTP check recorded separately; browser visual check not observed |
| Admin unauthenticated | Healthy gateway | Navigate to `/admin` | Redirect reaches Filament login page | Observed; see `infra/caddy/GATEWAY_INTEGRATION_EVIDENCE.md` for the login URL, title, and visible form |
| Admin authenticated | Development administrator created locally | Sign in at `/admin` | Dashboard renders and session remains same-origin | Not observed: no interactive development administrator was created |
| Livewire interaction | Authenticated Filament dashboard | Perform one non-destructive interaction (for example open/close user menu) and inspect Network | A request matching `/livewire-[^/]+/...` stays on Laravel; UI updates without console error | Not observed: authentication prerequisite unavailable |
| Filament assets | Unauthenticated `/admin/login` page | Reload with Network/assets visible | Filament CSS, JS, fonts and Livewire script return from Laravel through Caddy | Observed; exact CSS/font/JS/Livewire request URLs and `200` statuses are in `infra/caddy/GATEWAY_INTEGRATION_EVIDENCE.md` |
| Public media | Storage link exists and a non-secret marker is placed in `api_public_media` | Request `/storage/<marker>` | Marker is served by Laravel/Apache through Caddy and survives an API recreate | Observed by local HTTP check 2026-08-13: non-secret marker survived `api --force-recreate` and was then removed. Browser visual navigation was not repeated. |
| HMR | Web dev server running | Make a harmless local text edit in `web`, observe browser update, then revert it | Browser updates through gateway without full manual reload or WebSocket error | Not observed: manual browser step |
| System theme | Fresh browser context with OS light then dark preference | Open `/es` in each context | Initial theme follows system preference without visible flash | Not observed: manual browser step |
| Explicit and invalid theme | Existing `/es` page | Toggle theme; then set invalid `portfolio_theme` value in browser storage and reload | Explicit choice persists; invalid value is cleared and falls back to system preference | Partially observed 2026-08-13: selecting Dark changed the root theme and it remained dark after `/en` navigation. Invalid-storage fallback was not observed. |
| Hydration and console | `/es`, `/en`, and `/admin/login` loaded | Inspect DevTools console after each render and interaction | No hydration error or uncaught console error | Observed 2026-08-13: no captured warning/error console entry after each rendered state. |
| Gateway/API restart | Gateway/API healthy | Stop and start `api`, then reload `/api/v1` and `/es` | While API is stopped `/api/v1` is 502, never a Next page; API recovers after start | Observed by local HTTP check: API returned `502` while `/en` stayed `200`, then `/api/v1` recovered. Browser-level recovery render remains unobserved. |
| MySQL persistence | Existing known development database record | Restart `mysql`, then `api` and gateway if required | Record remains after restart | Observed by local application/query evidence 2026-08-13: a non-admin factory marker user survived `mysql` and `api --force-recreate` without volume removal, then was deleted. Browser visual navigation was not repeated. |
| Media persistence | Existing non-secret media marker | Recreate only `api`, then request marker | Marker remains available through `/storage/*` | Observed by local HTTP check 2026-08-13: `phase-3-verification-media` survived `api --force-recreate`, was returned through `/storage/`, then removed. Browser visual navigation was not repeated. |
| Clean restart | All manual checks complete | Restart the Compose development services without deleting volumes | Gateway, web, API, and MySQL recover; locale/API/admin remain reachable | Not observed: manual browser step |
